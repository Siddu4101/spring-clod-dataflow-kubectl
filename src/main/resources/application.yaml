spring:
  application:
    name: demo
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    .show-sql: true
  h2:
    console:
      enabled: true
      path: /console
